services:
    agent:
        extends: { file: agent/compose.yaml, service: agent }

    backend-php:
        extends: { file: backend/compose.yaml, service: backend-php }

    backend-database:
        extends: { file: backend/compose.yaml, service: backend-database }

    mercure:
        extends: { file: backend/compose.yaml, service: mercure }

networks:
    unlockey-agent: ~
    unlockey-backend: ~

volumes:
    database-data: ~
    caddy_data: ~
    caddy_config: ~
    mercure_data: ~
    mercure_config: ~
